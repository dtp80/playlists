# Server Configuration
PORT=3000
NODE_ENV=production

# PostgreSQL Database Connection
# Get your connection string from your PostgreSQL provider (e.g., Aiven, Heroku, Supabase)
DATABASE_URL="postgresql://username:password@host:port/database?sslmode=require"

# Session Secret
# Generate a secure random string for production (minimum 32 characters recommended)
SESSION_SECRET="your-secret-key-change-in-production-use-strong-random-string"

# Vercel Deployment Notes:
# 1. Add these environment variables in your Vercel project settings
# 2. DATABASE_URL should point to your cloud PostgreSQL database
# 3. SESSION_SECRET should be a strong random string (you can generate one using: openssl rand -base64 32)
# 4. Make sure your PostgreSQL database allows connections from Vercel's IP ranges
